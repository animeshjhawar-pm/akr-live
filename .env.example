# Copy this file to .env and fill in real values.
# On Vercel, set these as Environment Variables in the project settings (NOT in this file).

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=465512941999

# Read-only postgres connection string for resolving project_id -> project name.
DB_URL=postgres://USER:PASSWORD@gw-rds-prod.celzx4qnlkfp.us-east-1.rds.amazonaws.com:5432/gw_stormbreaker

# Filter mode (one of these):
STATE_MACHINE_PREFIX=stormbreaker-
# If set, pins to a single state machine by exact name (overrides the prefix filter).
STATE_MACHINE_NAME=stormbreaker-get-automated-researched-keywords-prod
